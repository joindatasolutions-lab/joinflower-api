name: CI Pytest

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      RUN_INTEGRATION_TESTS_SECRET: ${{ secrets.RUN_INTEGRATION_TESTS }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pytest
        run: pytest -q

      - name: Run integration guards
        if: ${{ env.RUN_INTEGRATION_TESTS_SECRET == '1' }}
        env:
          RUN_INTEGRATION_TESTS: "1"
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: pytest -q -m integration
